import { redirect } from "next/navigation";
export default function MarketingOverlordPage() { redirect("/#marketing-overlord"); }
export const metadata = { title: "Marketing Overlord | Barrios A2I", description: "Campaign autopilot" };
